apiVersion: v1
kind: Service
metadata:
  name: ip-reverser
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ip-reverser
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080